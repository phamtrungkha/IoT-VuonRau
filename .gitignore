# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
*.tmp
*.log

# Secrets (local overrides; keep *.example in repo if needed)
.env
.env.*
!.env.example

# PlatformIO / clangd generated files (may contain local build flags)
**/compile_commands.json

# Python venv (local only; recreate with: python3 -m venv .venv)
.venv/

# tools/
tools/__pycache__/
*.pyc
*.pyo
